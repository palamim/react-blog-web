import styled from 'styled-components';

import { Container } from '@/atomic/obj.grid/grid.style';

export const WriteWrapper = styled(Container)`
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
`;
